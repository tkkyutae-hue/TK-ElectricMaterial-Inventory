Do not redesign the Daily Report page. Keep the current layout and styling, but fix the input behavior and data consistency issues.

Fix Admin > Projects > Daily Reports > New Report with these exact changes:

1) General Info / Manpower / Materials input layout bugs
- Current input widths and field sizing are unstable and visually broken.
- Fix column widths so fields do not resize or shift unexpectedly while typing.
- Inputs must stay aligned in a fixed grid.
- Do not let field widths expand/shrink based on value length.

2) Worker name field issue
- In Manpower, worker names are getting cut off or displayed incorrectly.
- Fix the worker name column so names display cleanly.
- Long names should not break layout.
- Use stable width + ellipsis + tooltip/full hover title if needed.
- Do not duplicate or clip the selected name awkwardly.

3) Materials row unit field bug
- In Materials, when an item is selected, the Unit field width/layout changes left-right or expands/shrinks.
- Fix this so the Unit field width is fixed and visually stable.
- Unit text must always be centered and use the same font style regardless of item source.
- Inventory-linked item selection must not distort the row layout.

4) Materials inventory search consistency
- Materials item search should behave like inventory mode:
  - searchable
  - dropdown suggestions
  - select inventory item
  - auto-fill unit from inventory master
- Manual entry is still allowed if no inventory item is selected.

5) Equipment unit correction
- Equipment is currently using DAY as the default unit. This is incorrect for this use case.
- Equipment should default to EA because this section is counting equipment quantity (number of machines/equipment).
- Quick-add items such as:
  - Scissor Lift
  - Boom Lift
  - One Man Lift
  - Reach Forklift
  - Forklift
  - Trench
  - Excavator Small
  - Excavator Big
  must default to unit = EA
- Keep Hours Used as a separate field.

6) Language switch in Daily Report
- Add a language switcher to the Daily Report page top-right, consistent with the app header behavior.
- Supported languages:
  - English
  - Korean
  - Spanish
- Translate all Daily Report UI text, including:
  - section titles
  - labels
  - button text
  - placeholders
  - empty state text
  - status values
  - helper text
- The language switch must affect the page immediately without breaking current form data.

7) Submission validation
- Prevent submit if:
  - Prepared By is empty
  - Report Date is empty
- Show clear validation messages
- Save Draft should still work with partial input

8) Keep current report numbering logic
- Report No should remain auto-generated based on report count for that project
- Do not let users manually overwrite report numbering unless intentionally designed elsewhere

9) Keep current styling
- Do not redesign the whole page
- Only fix layout, consistency, and localization issues

Please test these cases:
- long worker names
- inventory-linked material
- manual material entry
- equipment quick-add row
- switching language after some inputs are already typed