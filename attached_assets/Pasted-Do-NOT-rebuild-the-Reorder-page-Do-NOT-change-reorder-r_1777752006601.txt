Do NOT rebuild the Reorder page.
Do NOT change reorder recommendation business logic or backend routes broadly.

Focus ONLY on:
- client/src/pages/Reorder.tsx
- tiny directly related helpers only if truly needed

TASK GOAL
Add a simple, readable usage-frequency indicator to the Reorder table so admins can quickly tell which items are actively being consumed.

IMPORTANT DIRECTION
Do NOT add a raw numeric frequency column first.
Use a compact status-style label instead.

REQUIRED CHANGE

PART 1 — ADD A NEW COLUMN
Add a new column:
- 사용빈도

This column should show one of exactly these three values:
- 잦음
- 보통
- 없음

PART 2 — DEFINE THE FREQUENCY RULE
Base the frequency value on recent transaction usage.

Use this rule:
- calculate frequency from the number of `issue` transactions in the last 30 days for that item
- classify as:
  - 잦음 = 8 or more issue transactions in the last 30 days
  - 보통 = 1 to 7 issue transactions in the last 30 days
  - 없음 = 0 issue transactions in the last 30 days

Do NOT include:
- receive
- return
- adjust
- internal transfer
unless the current backend already has a clearly defined consumption-specific rule that is safer and equivalent

PART 3 — DISPLAY STYLE
Render 사용빈도 as a compact badge/chip, not plain long text.

Requirements:
- keep it visually lightweight
- do not overpower priority/status colors
- keep the table readable and admin-friendly

If easy and low-risk, add hover/title support:
- 잦음 → 최근 30일 N회 출고
- 보통 → 최근 30일 N회 출고
- 없음 → 최근 30일 출고 없음

PART 4 — KEEP PAGE STRUCTURE STABLE
- keep the table/list layout
- keep the photo column
- keep supplier out of the main table
- keep search/filter structure
- do NOT redesign the page into cards
- do NOT add grouped-by-category rendering

PART 5 — DATA / IMPLEMENTATION SCOPE
If the reorder API already returns enough transaction-derived data safely, use it.
If not, add only the smallest safe backend support needed to provide the 30-day issue-count per item.

Do NOT do a broad backend rewrite.
Keep changes narrow and practical.

OUTPUT
1. Summary of the new 사용빈도 column
2. Exact classification rule used
3. Whether any backend support was added
4. Files modified
5. Anything intentionally left unchanged