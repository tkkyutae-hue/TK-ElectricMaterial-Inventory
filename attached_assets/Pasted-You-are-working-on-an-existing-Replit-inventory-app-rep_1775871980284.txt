You are working on an existing Replit inventory app repository.

IMPORTANT NON-DESTRUCTIVE RULES
1. Do NOT modify, recalculate, or backfill inventory quantities.
2. Do NOT edit, delete, migrate, rename, or reinterpret historical project records, daily report records, or inventory movement records.
3. Do NOT change business logic unless a tiny compatibility-safe fix is absolutely necessary.
4. Preserve current route behavior, API behavior, and current user-visible features.
5. This task is ONLY for refactoring and decomposing:
   client/src/pages/project/tabs/OverviewTab.tsx
   (or the actual current OverviewTab file path in this repo)

TASK GOAL
Refactor OverviewTab.tsx into smaller responsibility-based components and helpers so it becomes easier to maintain, while preserving current behavior and project/dashboard-style summary meaning.

IMPORTANT
- Do NOT rebuild the page from scratch
- Do NOT move one giant JSX block into one new giant file
- Prefer true responsibility-based decomposition
- Reuse existing shared UI components already in the repo wherever appropriate
- Keep the existing visual style and behavior as much as possible

==================================================
PART 1 — INSPECT CURRENT RESPONSIBILITIES
==================================================

Before editing, inspect OverviewTab.tsx and identify the main responsibilities currently mixed together.

Typical responsibilities to separate may include:
- overview header / title / quick actions
- KPI or summary stat strip
- project metadata / status summary cards
- activity / recent updates panel
- progress or health summary section
- team / contact / key info panel
- timeline or milestone summary block
- formatting / derived summary helpers
- empty / loading / error states

Do not rewrite logic unnecessarily.
Only separate responsibilities that are currently mixed into the giant file.

==================================================
PART 2 — DECOMPOSE BY RESPONSIBILITY
==================================================

Refactor OverviewTab.tsx into focused subcomponents and/or small helper files where appropriate.

Examples of good targets:
- OverviewHeader
- OverviewStatsSection
- OverviewProjectSummaryCard
- OverviewProgressSummary
- OverviewActivitySection
- OverviewTeamSummary
- OverviewMetaGrid
- OverviewEmptyState
- small utils/formatters file if needed

Requirements:
1. Keep the current behavior intact.
2. Reduce the size and complexity of OverviewTab.tsx significantly.
3. Prefer a few focused components rather than many tiny unnecessary files.
4. Avoid prop explosion where possible.
5. Keep shared state in the parent only when truly needed.
6. Move render-only and display-only logic down into child components when safe.

==================================================
PART 3 — REUSE SHARED UI WHERE APPROPRIATE
==================================================

Reuse existing shared components already present in the repo wherever appropriate, such as:
- PageHeader
- SectionCard
- EmptyState
- StatCard
- shared badges

Requirements:
1. Do NOT force shared components where they clearly do not fit.
2. Use them where they reduce repeated styling and improve consistency.
3. Keep Admin density and readability balanced.
4. Preserve the current overview page feel.

==================================================
PART 4 — KEEP BUSINESS LOGIC STABLE
==================================================

Very important:
- Do NOT change project overview meaning
- Do NOT change KPI/stat meaning
- Do NOT change backend contracts
- Do NOT change persistence behavior
- Do NOT alter how overview data is computed unless a tiny compatibility-safe mapping cleanup is absolutely necessary

If the file currently includes derived calculations or formatting logic:
- extract only small pure helpers where safe
- preserve output exactly as much as possible

==================================================
PART 5 — CLEAN UP LOCAL COMPLEXITY
==================================================

Improve maintainability by doing small local cleanup where safe:
- reduce giant inline JSX blocks
- reduce repeated card/style fragments
- extract repeated metadata rows
- extract repeated badge/status rendering
- extract repeated formatter logic
- simplify conditional rendering where possible

But:
- do NOT redesign the page
- do NOT introduce a brand new architecture
- do NOT broaden scope into other project tabs

==================================================
PART 6 — DO NOT SPREAD THE COMPLEXITY
==================================================

Important:
Do NOT “fake refactor” by creating one new giant extracted file that is still 600+ lines and holds most of the original complexity.

Prefer:
- one lean parent OverviewTab component
- a few medium-sized focused components
- tiny helper/util files only where useful

The goal is real maintainability improvement.

==================================================
PART 7 — KEEP UX INTACT
==================================================

Requirements:
1. Preserve existing summary cards and stat behavior
2. Preserve existing activity/progress rendering behavior
3. Preserve existing action buttons and navigation behavior
4. Preserve current empty/loading/error states
5. Preserve current layout intent even if internals are decomposed

If a tiny UI cleanup is needed for clarity, keep it minimal and consistent with the current Admin/project style.

==================================================
PART 8 — CONSTRAINTS
==================================================

1. Do NOT touch ScopeItemsTab in this task
2. Do NOT touch MovementForm in this task
3. Do NOT change server routes in this task
4. Do NOT change inventory calculations in this task
5. Keep the work incremental and scoped
6. Prefer safe refactor over clever refactor

==================================================
OUTPUT
==================================================

Provide:
1. Summary of the OverviewTab refactor
2. Files created
3. Files modified
4. Which responsibilities were extracted
5. Notes on what was intentionally left unchanged for safety
6. Whether OverviewTab is now lean enough to move on to the next step