You are working on an existing Replit inventory app repository.

IMPORTANT NON-DESTRUCTIVE RULES
1. Do NOT modify, recalculate, or backfill existing inventory quantities.
2. Do NOT edit, delete, migrate, rename, or reinterpret historical inventory movement records.
3. Do NOT bulk-update existing item records or reel data.
4. Do NOT delete legacy reel records.
5. Preserve compatibility with existing stored data and current app behavior.
6. This task is focused on enforcing the existing reel eligibility rule safely.

TASK GOAL
Complete the reel-rule work by doing two things:
1. Enforce reel eligibility on the server for NEW reel-related operations
2. Add a read-only legacy reel audit view/report for admins

IMPORTANT
- A shared reel eligibility helper already exists and has been improved
- Reuse the existing reel eligibility logic instead of creating a second conflicting rule
- Do NOT rebuild the inventory engine
- Do NOT migrate old data

FILES TO FOCUS ON
Inspect the repo and update only the relevant files for:
- shared reel eligibility usage
- server routes / handlers for reel-related actions
- any storage/service layer used by reel creation / receive / issue flows
- admin-facing audit screen or report surface
- any tiny shared helper needed for the audit only

==================================================
PART 1 — SERVER-SIDE REEL ENFORCEMENT
==================================================

Find the real server entry points used for reel-related operations, such as:
- reel creation
- reel receive flows
- reel issue / allocation flows
- any endpoint that creates or mutates reel-related records
- any item/update flow that enables reel-specific behavior for new data

Use the existing routes/flows.
Do NOT invent an entirely new route structure unless absolutely necessary.

Apply the existing reel eligibility rule server-side so NEW reel-related operations are blocked for non-eligible items.

Business rule:
Reel behavior is allowed ONLY for true reel-eligible items, according to the existing shared reel eligibility logic.
Non-eligible examples include:
- EMT
- Rigid / IMC / RMC
- PVC conduit
- Cable Tray
- accessory/fitting/hardware items such as connectors, bushings, couplings, etc.

Requirements:
1. Validate reel eligibility before reel-related persistence happens.
2. Return a clear 400-level error when a non-eligible item attempts a new reel-related operation.
3. Keep the error response simple and frontend-friendly.

Preferred error shape:
{
  "message": "Reel operations are not allowed for this item type",
  "errors": [
    { "field": "itemId", "message": "This item is not reel-eligible" }
  ]
}

Important:
- Do NOT break historical records
- Do NOT delete legacy reel data
- Do NOT retroactively invalidate old rows
- This is forward-only enforcement for NEW operations

==================================================
PART 2 — READ-ONLY LEGACY REEL AUDIT
==================================================

Add an admin-only read-only audit view or report that helps identify items whose legacy reel-related data may conflict with the current reel eligibility rules.

Goal:
Show admins which items currently have reel-related data in storage but are no longer eligible under the corrected business rule.

Requirements:
- Read-only only
- No bulk actions
- No automatic fixes
- No destructive operations

The audit should identify items such as:
- non-eligible items that still have legacy reel records
- accessory items inside Cable/Wire that previously surfaced reel behavior
- EMT / Rigid / PVC / Cable Tray items with reel-related stored data

Suggested columns:
- Item Name
- SKU
- Category
- Subcategory
- Reel Eligible (current rule)
- Legacy Reel Count
- Qty On Hand
- Reason / classification note

Suggested reason examples:
- "Accessory in Cable/Wire"
- "EMT family"
- "Cable Tray item"
- "Non-flex conduit"
- "Legacy reel data on non-eligible item"

Implementation guidance:
- Prefer a compact admin table or report surface
- Keep it simple and functional
- Reuse existing admin page patterns/components if possible
- Admin-only access pattern should follow existing repo conventions

==================================================
PART 3 — KEEP FIELD AND ADMIN CONSISTENT
==================================================

Make sure the server enforcement aligns with the current Field/Admin UI gating.

Requirements:
- Eligible items: reel operations continue to work
- Non-eligible items: UI stays hidden and server also rejects new reel operations
- Legacy data remains in storage but is audit-visible rather than user-actionable

==================================================
PART 4 — CONSTRAINTS
==================================================

1. Do NOT modify stored inventory quantities
2. Do NOT change historical movement meaning
3. Do NOT rebuild storage.ts broadly
4. Do NOT create duplicate reel-rule systems
5. Keep changes incremental and compatibility-safe
6. Prefer reusing existing shared reel eligibility logic everywhere

==================================================
OUTPUT
==================================================

Provide:
1. Summary of server-side reel enforcement changes
2. Summary of the legacy reel audit view/report
3. Files created
4. Files modified
5. Notes on which reel-related routes/flows were protected
6. Notes on what was intentionally left untouched