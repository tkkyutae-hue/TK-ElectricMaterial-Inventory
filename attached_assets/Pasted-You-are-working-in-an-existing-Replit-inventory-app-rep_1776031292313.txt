You are working in an existing Replit inventory app repository.

THIS TASK IS STRICTLY SCOPED.
Fix only the mobile layout of the reel-entry UI in MovementForm.

==================================================
FILES YOU MAY MODIFY
==================================================

1. client/src/components/MovementForm.tsx

Only if strictly necessary for the exact dropdown viewport behavior:
2. client/src/components/ui/select.tsx

If you think any other file must be changed, STOP and explain why before changing it.

==================================================
FILES YOU MUST NOT MODIFY
==================================================

- client/src/components/layout/FieldLayout.tsx
- client/src/pages/field/FieldInventory.tsx
- client/src/pages/field/FieldTransactions.tsx
- client/src/pages/field/FieldCartReview.tsx
- client/src/pages/field/FieldRequestsList.tsx
- client/src/components/ui/command.tsx
- client/src/hooks/use-mobile.tsx
- client/src/lib/fieldTokens.ts
- client/src/index.css
- any backend file
- any routes file
- any schema/storage file
- any admin mode file
- any toast logic
- any request logic

==================================================
GOAL
==================================================

In cable/reel-related MovementForm flows, the "NEW REELS" entry UI is breaking on mobile because too many columns are forced into one row.

Fix ONLY the mobile reel-entry layout.

==================================================
REQUIRED RESULT
==================================================

On mobile only:
- do NOT keep the reel input row as a desktop-style one-line multi-column grid
- reflow it into a safer 2-row or compact card layout

Recommended mobile grouping:
Row 1:
- Reel ID
- Length FT

Row 2:
- Brand
- Location
- Status

Row 3 / footer area:
- row actions such as remove / add row / add all reels as appropriate

Requirements:
1. Prevent fields from overflowing outside the container
2. Prevent dropdowns from overlapping buttons awkwardly
3. Keep desktop reel layout unchanged if it already works
4. Preserve all reel data logic and existing behavior
5. Do NOT redesign the whole form

==================================================
SUCCESS CONDITION
==================================================

This task is successful only if:
- only allowed files were modified
- mobile reel-entry no longer overflows horizontally
- dropdowns no longer collide with adjacent controls/buttons
- desktop behavior remains unchanged

==================================================
OUTPUT FORMAT
==================================================

When done, report:
1. Exact files changed
2. How the mobile reel-entry layout was reflowed
3. How dropdown overlap/overflow was prevented
4. Confirmation that no other files were modified